version: '3'

services:
  model-express-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - SERVER_PORT=8000
      - LOG_LEVEL=info
    restart: unless-stopped
