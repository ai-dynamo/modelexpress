apiVersion: v1
kind: Service
metadata:
  name: { { include "model-express.fullname" . } }
  labels: { { - include "model-express.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "model-express.selectorLabels" . | nindent 4 } }
